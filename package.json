{
  "name": "scentora-server",
  "version": "1.0.0",
  "main": "db.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "author": "aswin",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
